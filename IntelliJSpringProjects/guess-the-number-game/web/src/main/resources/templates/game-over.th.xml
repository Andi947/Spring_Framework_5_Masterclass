<thlogic>
   <attr sel="#headId"
         th:replace="~{fragments/head :: headFragment}"/>
   <attr sel="#result-message"
         th:text="${__${T(com.company.util.AttributeNames).RESULT_MESSAGE}__}"/>
   <attr sel="#restart"
         th:href="@{__${T(com.company.util.GameMappings).RESTART}__}"/>
   <attr sel="#home"
         th:href="@{__${T(com.company.util.GameMappings).HOME}__}"/>
   <attr sel="#footerId"
         th:replace="~{fragments/footer :: footer}"/>
</thlogic>